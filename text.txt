working directory -- > staging area --> local repo.      --> remote repo.
test-project      --> git add       --> git commit       --> git push

ls 
ls -a
git config
git config --global
git config --list
git reset --> 
git reset --soft HEAD^
git reset --hard HEAD^
git log 
git log --online
git checkout  commitId
git checkout master
git commit -m ""
git remote -v
git push -u origin (url)
git pull origin master
git branch 
git branch -r
git branch -a
git branch(branch name) ---> to create a branch
git checkout (branch name) ---> to work with a branch

git merge (branch)---> to merge to master branch
git push ---> to push to remote repo
git push -u origin (branch name)--> to push a branch to master or remote repo without merge(so called pull request)
git checkout master
git pull --> to get all updated codes
From master branch --- git merge branchname---> two way merge


tracked
untracked

git diff
git restore 

changes control 
version control 